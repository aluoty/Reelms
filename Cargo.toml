[package]
name = "fishing_game"
version = "0.1.0"
edition = "2021"
description = "Fishing Game — Rust + Bevy 2D port"
license = "MIT"

[dependencies]
bevy = "0.16"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
